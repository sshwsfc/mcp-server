---
description: AIRIOT 平台文件管理功能 - 上传、下载、删除平台文件，支持文件列表查询和文件操作
---

## 文件管理

### 上传文件

**命令：**

```bash
airiot file-upload <filePath> [选项]
```

**选项：**

| 选项 | 描述 |
|------|------|
| `--name <name>` | 文件名（默认使用原文件名） |
| `--mime <type>` | MIME 类型 |

**常用 MIME 类型：**

| 文件类型 | MIME 类型 |
|----------|-----------|
| PDF | application/pdf |
| JPEG 图片 | image/jpeg |
| PNG 图片 | image/png |
| JSON | application/json |
| 文本文件 | text/plain |
| Excel | application/vnd.openxmlformats-officedocument.spreadsheetml.sheet |

**示例：**

```bash
# 上传文件（使用原文件名）
airiot file-upload ./report.pdf

# 上传并指定新文件名
airiot file-upload ./data.json --name config.json

# 指定 MIME 类型
airiot file-upload ./image.jpg --mime image/jpeg
```

### 获取文件信息

**命令：**

```bash
airiot file-info <id>
```

**参数：**

- `id`：文件 ID

**示例：**

```bash
airiot file-info file-001
airiot file-info file-001 -o json
```

### 删除文件

**命令：**

```bash
airiot file-delete <id>
```

**参数：**

- `id`：文件 ID

**示例：**

```bash
airiot file-delete file-001
```

---
